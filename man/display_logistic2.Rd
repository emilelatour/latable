% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display-logistic.R
\name{display_logistic2}
\alias{display_logistic2}
\title{Display Logistic Regression Results (given variable names)}
\usage{
display_logistic2(
  data,
  outcome,
  predictors,
  event_level = NULL,
  add_multi = FALSE,
  format = "html",
  conf_level = 0.95,
  exponentiate = TRUE,
  include_last_row = TRUE
)
}
\arguments{
\item{data}{A tibble or data frame.}

\item{outcome}{Character string. Outcome variable name.}

\item{predictors}{Character vector of predictor variable names.}

\item{event_level}{Character string. The value of the outcome variable to
be modeled as the positive (event) class. When supplied, the model is fit
with \code{(outcome == event_level)} as the response. When \code{NULL} (default),
R's default factor coding determines the positive class.}

\item{add_multi}{Logical; include multivariable results. Default is \code{FALSE}.}

\item{format}{Display format. Default is \code{"html"}.}

\item{conf_level}{Confidence level. Default is \code{0.95}.}

\item{exponentiate}{Logical; exponentiate to ORs. Default is \code{TRUE}.}

\item{include_last_row}{Logical; add blank spacing row. Default is \code{TRUE}.}
}
\value{
A tibble containing logistic regression results.
}
\description{
Given a string \code{outcome} and a character vector \code{predictors}, displays
univariable and (optionally) multivariable logistic regression results in a
presentation-ready format.

This version lets you specify outcome and predictors explicitly, without
needing a pre-built fit object. An optional \code{event_level} controls which
value of the outcome variable is modeled as the positive (event) class.
}
\examples{
\dontrun{
display_logistic2(data = infert,
                  outcome = "case",
                  predictors = c("induced", "spontaneous", "education"),
                  add_multi = TRUE)

# With explicit positive class
display_logistic2(data = predimed,
                  outcome = "htn",
                  predictors = c("sex", "bmi", "smoke"),
                  event_level = "Yes",
                  add_multi = TRUE)
}
}
